SSEG SEGMENT
	ID DW 41,26,35
	NUM DW 5
	RESULT DW 0
SSEG ENDS

CSEG SEGMENT
	ASSUME CS:CSEG,DS:SSEG
	FIB PROC NEAR
		CMP BX ,1
		JE FIB1
		CMP BX,2
		JE FIB2
		PUSH BX
		DEC BX
		CALL FIB
		POP BX
		MOV CX,AX
		ADD AX,DX
		MOV DX,CX
		RET
	FIB1:
		MOV AX,1
		RET
	FIB2:
		MOV AX,1
		MOV DX,1
		RET
	FIB ENDP
	
	MAIN PROC far
	START:
		MOV AX,SSEG
		MOV DS,AX
		XOR BX,BX
		MOV BL, NUM
		CALL FIB
		MOV RESULT,AX
		MOV AX,4C00H
		INT 21H	
	MAIN ENDP
CSEG ENDS
END START